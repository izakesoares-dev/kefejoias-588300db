import { Product } from "./products";

// Placeholder — add real earring product images as they become available
// For now we export empty arrays so pages render without errors.

export const stoneEarrings: Product[] = [];
export const resinEarrings: Product[] = [];
